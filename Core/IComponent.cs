namespace RaymarchEngine.Core
{
    /// <summary>
    /// An updateable that is attached to a gameobject rather than living on its own
    /// </summary>
    public interface IComponent : IUpdateable
    {
    }
}